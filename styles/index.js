import theme from './theme';
import base from './base';
import StyledHeader from './header';
import StyledFooter from './footer';
import StyledWrapper from './wrapper';

export { theme, base, StyledHeader, StyledFooter, StyledWrapper };
